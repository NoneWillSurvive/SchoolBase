To work with the site, enter the following at the command line:
1. npm install
2. npm start
The server runs on http://localhost:3000
DBMS - PostgreSQL 10+
You also need an Internet connection, because JQuery scripts are enabled online.

Для работы с сайтом необходимо в командной строке ввести:
1. npm install
2. npm start
Сервер работает на http://localhost:3000
СУБД - PostgreSQL 10+
Необходимо также подключение к интернету, т.к. JQuery-скрипты поключаются онлайн.